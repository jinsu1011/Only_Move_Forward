# 이어서 작업하기 — 전진만할게요

> 다른 노트북이나 새 대화에서 이어받을 때 이 파일부터 연다. 옮겨지는 건 파일뿐이니 작업을 끝낼 때마다 갱신한다.
> 마지막 갱신: 2026-09-16

## 1단계 — 노트북에 앉으면

```bash
git pull
./시작.sh
```

`준비 완료`가 나오면 끝. 설치물(`web/frontend/deps.nosync`, `web/backend/.venv.nosync`)은 iCloud 동기화에서 빠져 있어 노트북마다 새로 만든다.
노트북마다 사용자 폴더 이름이 다를 수 있으니(`kimjinsu`, `kimjinsoo`) 절대 경로를 문서·설정에 넣지 않는다.

### 실행 — 창 두 개

```bash
cd web/backend && ./.venv/bin/python -m uvicorn app.main:app --port 8010
```

```bash
npm --prefix web/frontend run dev
```

- 브라우저: http://localhost:5180 · API 문서: http://localhost:8010/api/docs
- **센서는 데스크톱 Chrome/Edge 에서만**(Web Serial). 포트는 한 프로그램만 열 수 있으니 아두이노 IDE 시리얼 모니터·다른 탭을 닫는다
- `--reload` 는 쓰지 않는다(iCloud 폴더에서 불안정)
- AI 키: `web/backend/.env.example` → `.env` 로 복사 후 `LLM_API_KEY` 등 입력. 없으면 규칙 기반 요약

## 2단계 — 떠나기 전에

```bash
./정리.sh
git add -A && git commit -m "무엇을 했는지 한 줄" && git push
```

## 3단계 — 지금 어디까지 왔나

`기획/PROGRESS.md` 참고. 요약:

- 웹 MVP 동작 · pytest 14 · vitest 20 통과 · 빌드 성공
- 제출물 `제출/최종본/` — API.yml·DBML(계약 검증 불일치 0), 개요 PDF 초안 25쪽. 확인 항목은 `제출/최종본/체크리스트.md`
- **남은 일**
  1. 회사에서 센서 실측: 중앙 3초 → 좌 → 중앙 → 우 → 중앙 → READY, roll 은 조향만·↑/↓ 는 전진·후진만, 케이블 분리 시 즉시 정지 → 재연결 후 정렬 복귀, 민감도 프리셋 확정
  2. 반·고유번호·이름 확인, dbdiagram.io·Swagger Editor 직접 확인, PDF 검토·발표 리허설
  3. 마감 2026-09-17(목) 13:40 업로드

## 검증 명령

```bash
cd web/backend && ./.venv/bin/python -m pytest -q
cd web/frontend && npm run typecheck && npm test && npm run build
```

골든 기록(`web/frontend/src/sim/golden.json`)은 백엔드 테스트가 다시 만든다. 주행 엔진(`web/backend/app/sim.py` ↔ `web/frontend/src/sim/engine.ts`)을 바꾸면 양쪽을 함께 바꾸고 두 테스트를 모두 돌린다.

API·DB 를 바꿨다면 제출 명세도 고치고 계약 검증을 다시 돌린다(`제출/최종본/체크리스트.md` 3절).
