# 진행 상황

> 완료 · 진행 중 · 다음 작업만 적는다. 결정 배경은 `../PROJECT_HEAD.md`.
> 최종 수정: 2026-09-16

## 완료

### 조사·설계
- [x] 참고자료 5개 복사·해시 대조, 평가 체크리스트 26항목 (`기획/이전기록/평가/rubric-checklist.md`, 현재는 `제출/최종본/체크리스트.md`)
- [x] 별표26 감점 46·실격 11 조사와 A/B/C 지원 분류 (`data/road-rule-research.json`, `기획/설계/road-test-coverage.md`)
- [x] 화면 흐름·와이어프레임, UI↔API↔ERD 화면 단위 대조, API·DBML 초안 (`제출/`)

### 웹 구현 (`web/`)
- [x] 인증: scrypt 해시, HttpOnly 세션 쿠키, CSRF(double-submit + Origin), 로그인 시도 제한, 본인 기록만 조회
- [x] 필기: 주제·문항 수 선택, 답안 즉시 저장, 제출 후에만 정답 공개, 주제별 결과, 오답 해설 + AI 해설
- [x] 주행: 기본조작 연습장(전진·후진 정차), 도로 A코스(신호·보호구역·횡단보도), Three.js 화면
- [x] 서버 재생 검증: 입력 기록 → 서버 엔진 재생 → 사건 확정, 브라우저 결과와 일치 여부 표시
- [x] AI 리포트: 서버 계산 통계 + AI 서술(인용·수치 검증), 키 없으면 규칙 기반
- [x] 서비스명 **전진만할게요** 반영

### 2026-09-15 오후 수정
- [x] 센서 민감도 보정: 튐 제거 + 지수 평활 + 방향 유지 시간 + 둔감/보통/민감 프리셋(기본 15°/8°·150ms·120ms). 정렬·주행 화면 동일 적용, 보정값을 주행 기록에 저장
- [x] 정렬 반복 줄이기: 탭 단위 30분 정렬 재사용(중앙 0.4초 확인만), 키보드 확인 기억, 민감도 변경 시 재정렬 없음
- [x] 사건 누적: 실격 해당 사건도 주행을 끝내지 않음. 도로 이탈 시 차로 복귀. 완주·실격 사유 포함 상태 구분
- [x] Esc: 일시정지 메뉴 → 한 번 더 누르면 기록 저장 후 나가기
- [x] 폴더 구조를 미니프로젝트1 방식으로 정리(기획/·제출/·참고자료/·data/·hardware/, 시작.sh·정리.sh)

### 검증 기록 (2026-09-15)
- 백엔드 `pytest` 14 passed — 인증·CSRF·필기 정답 은닉·재시도 키·주행 재생(일치/조작 시 불일치)·사건 누적·도로 이탈 복귀·AI 대체 경로·수치 검증
- 프런트 `vitest` — 파서·히스테리시스·**민감도 필터(손떨림 무시·의도적 기울기 0.3초 내 반응·충격 무시·프리셋 순서)**·정렬 상태 기계·정렬 재사용·키 매핑·**Python↔TS 엔진 골든 3종 일치**
- `tsc -b` 오류 0, `vite build` 성공

### 2026-09-16 제출물 (`제출/최종본/`, 사용자 지시로 개요 PDF 초안까지 작성)
- [x] `9반_P286_김진수_전진만할게요-API.yml` — 실제 서버 코드 기준 새로 작성. swagger-parser 통과(24경로·27동작·스키마 54), 로컬 Swagger UI 렌더 오류 0
- [x] **계약 검증**: 임시 DB로 앱 실행 → 27개 동작 전부·53건 응답을 명세로 검증(선언 안 된 필드 금지) → 불일치 0. 필드 추가·타입 변경한 응답은 실패함을 역검증
- [x] `…-DB.dbml` — `db.py` 스키마 기준. @dbml/core 파싱 오류 0(20테이블·27관계·15 Enum·그룹 5), ERD 이미지 로컬 렌더
- [x] 실제 앱 화면 캡처 23장(`자료/캡처/`, 가상 계정). 실제 LLM 해설·리포트 응답 화면 포함 → AI 실제 호출 확인
- [x] `…-개요.pdf` 초안 25쪽(원본 `개요PDF/slides.html`)
- [x] 결함 수정: 주행 결과 경로 지도가 검게 비던 문제(ResizeObserver 가 캔버스를 지운 뒤 재렌더 안 함, `render3d.ts`) · 참고 감점 0점이 “-0”으로 보이던 문제(`DriveResult.tsx`)
- 재검증: `tsc -b` 오류 0, vitest 20 passed, pytest 14 passed(수정 전 실행), 수정 후 캡처로 지도 표시 확인
- 저장소 정리: 구현 전 설계 초안·평가 감사·이어가기 프롬프트·`제출/design.*` 를 `기획/이전기록/` 로 이동(보존), README·PROJECT_HEAD·이어서작업 현재 상태로 갱신, `시작.sh`·`정리.sh` 실행 권한

## 진행 중 / 사용자 확인 필요
- [ ] 실제 핸들로 좌·우 방향, 민감도 프리셋 확정 (회사에서 센서 실측)
- [ ] 반·고유번호·이름(9반/P286/김진수) 최종 확인 — 첫 번째 예시 값 사용
- [ ] dbdiagram.io · Swagger Editor 직접 붙여 넣어 확인(외부 사이트라 사용자 수행)
- [ ] 개요 PDF 내용 검토·발표 리허설(4분30초~5분)
- [ ] 마감 2026-09-17(목) 13:40 업로드

## 다음 작업
1. 사용자 PDF 검토 의견 반영
2. 센서 실측 후 기본 프리셋 확정 → 필요 시 캡처(센서 정렬 READY) 추가
3. Three.js 번들 분리(837kB 경고, 비차단)
