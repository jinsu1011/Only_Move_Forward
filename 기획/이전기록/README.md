# 이전 기록 — 현재 기준 아님

구현을 시작하기 전(2026-09-15)에 만든 설계 초안과 평가 문서를 이력으로 보존한다. **지금 서비스·제출물의 기준은 실제 코드와 `제출/최종본/`이다.**

| 폴더 | 내용 | 현재와 다른 점 |
|---|---|---|
| `설계초안/` | UI 흐름·와이어프레임 17화면, API·DB 설계 제안, 화면 단위 UI↔API↔ERD 행렬 | 구현은 화면 13개. 경로(`/scenarios/{id}/rules` 등)·필드가 다름. Q&A·면허 안내 전용 화면 없음 |
| `제출초안/` | `design.yml`·`design.json`·`design.dbml` | 서비스명 “면허핏”, 구현 전 경로. 제출 대상 아님 |
| `근거/` | 초안 생성 스크립트·검증 기록·화면 계약·와이어프레임 캡처 | 스크립트는 초안을 다시 생성하므로 실행하지 않는다 |
| `평가/` | 품질 감사·평가 체크리스트 26항목·보고서 작성 가이드 | 제출 확인은 `제출/최종본/체크리스트.md` 로 대체 |
| `프롬프트/` | 이전 이어가기 프롬프트 | 다른 노트북 절대 경로·구현 전 상태 기준. 이어받기는 루트 `이어서작업.md` |
