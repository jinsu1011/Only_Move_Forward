# 제출 체크리스트 — 9반_P286_김진수_전진만할게요

> 기준: `참고자료/9반_평가기준.pdf`, `참고자료/과제안내_AI웹서비스설계_Mini-project.pdf`
> 마감: **2026-09-17(목) 13:40** — 세 파일이 모두 올라간 시각 기준. 구글 드라이브 공유 폴더에 업로드(재업로드 가능, 마지막 파일이 최종본)
> 작성: 2026-09-16. 업로드·제출은 사용자가 직접 한다.

## 1. 제출 파일

| 파일 | 상태 | 검증 |
|---|---|---|
| `9반_P286_김진수_전진만할게요-개요.pdf` | 25쪽 (원본 `개요PDF/slides.html`) | Chrome으로 PDF 생성, 이미지 누락 0, 세로·가로 넘침 0, 내용 검토 완료(2026-09-16) |
| `9반_P286_김진수_전진만할게요-API.yml` | 실제 서버 코드 기준 작성 | swagger-parser validate 통과(24경로·27동작·스키마 54) · 로컬 Swagger UI 렌더 오류 0 · 실제 응답 53건 대조 불일치 0 |
| `9반_P286_김진수_전진만할게요-DB.dbml` | 실제 DB 스키마(`web/backend/app/db.py`) 기준 | @dbml/core 파싱 오류 0 (20테이블·27관계·15 Enum·그룹 5) |

- [x] **파일명 규칙** — 9반 평가기준 6쪽 `{반}_{고유번호}_{이름}_{프로젝트명}-개요.pdf`(예시 `9반_P000_홍길동_회의실예약`)와 세 파일 모두 일치. 2026-09-16 확인
- [x] **고유번호 P286** — 사용자 확인 완료(2026-09-16). 직전 과제(WME) 제출 때와 동일 값
- [ ] 👤 프로젝트명 표기 `전진만할게요` — 한글 파일명이 드라이브에서 깨지지 않는지 업로드 후 확인
- [x] **dbdiagram.io 렌더** — 2026-09-16 실제 사이트에 붙여 넣어 확인. **오류 0**, 20테이블 전체 렌더링. 경고 3건은 아래 참고
- [x] **Swagger Editor(editor.swagger.io) 파싱** — 2026-09-16 실제 사이트에서 확인. **오류 0**, 27동작·5태그·54스키마 렌더(로컬 검증치와 일치)
- [x] PDF 내용 검토 — 배경 수치 출처 표기 있음(4쪽 하단: 별표26 2026-02-24 개정·2026-08-01 시행 / 도로교통공단 조회 2026-09-15). 57 = 감점 46 + 실격 11 을 `data/road-rule-research.json` 과 대조해 일치 확인. 조판 결함 1건 수정(아래 4절)
- [x] 발표 대본 작성 — `발표대본.md`. 슬라이드별 시각 배분, 분량 약 1,590음절 ≈ 4분 40초. **리허설 자체는 사용자가 수행**
- [ ] 👤 발표 리허설 2회 — 타이머로 4분 30초~5분 확인
- [x] 제출물에 실제 개인정보·기업 정보 없음 — API.yml·DBML·slides.html·PDF 텍스트 전수 검사. 이메일은 `learner@example.com` 하나뿐, 실제 계정·연락처·기업 정보 0건

### dbdiagram.io 경고 3건 — 확인 후 그대로 둠

`official_scoring_rules.category_id` · `driving_events.rule_id` · `ai_jobs.answer_id` 세 개가 nullable 인데 `ref: >` 로 적혀 있다는 경고다.
세 컬럼 모두 `db.py` 에서 실제로 nullable 이 맞다(각각 134·193·206행). dbdiagram.io 는 `>?` 로 바꾸라고 제안한다.

**바꾸지 않았다.** `>?` 는 dbdiagram.io 전용 확장이라 공식 파서가 거부한다 — `@dbml/core` 3.14.1 과 최신 10.1.1 모두 `Expected " " but "?" found` 로 파싱 실패했고, 인라인 `[ref: >? ...]` 와 독립 `Ref:` 블록 양쪽에서 실패한다.
접수 요건은 "dbdiagram.io 에서 오류 없이 렌더링"이고 현재 오류는 0이다. nullable 여부는 컬럼 정의에 `not null` 이 없는 것과 각 컬럼 Note 로 이미 드러나 있다.

## 2. 평가 항목 대응

| 평가 | 개요 PDF 위치 | 근거 파일 |
|---|---|---|
| 서비스 목적·타깃·문제 정의(원인·배경·현황) | 3~6쪽 | — |
| 액터별 기능 ↔ 화면 연결 | 7~8쪽 (FR 18개, 화면 ID·API) | API.yml `[화면]` 설명 |
| 전체 UI 흐름 · 화면 구성 요소 · 화면 캡처 | 9~18쪽 (화면 13개, 캡처 21장) | `자료/캡처/` |
| 예외·오류 시나리오 (가점) | 10·12~19쪽 빨강 표기, 19쪽 표 | API.yml 오류 응답 |
| UI ↔ API 필드 일치 | 각 화면 슬라이드 파랑 표기, 22쪽 대응표 | 계약 검증(아래) |
| 데이터 모델: 엔티티·PK/FK·M:N·타입·제약 | 20~22쪽 | DB.dbml |
| API: 전체 목록·요청/응답/예외·`$ref` 재사용 | 23~24쪽 | API.yml |
| 과도한 범위 회피 | 6쪽 범위(포함/제외) | — |

## 3. 검증 방법 (재실행)

도구는 `검증도구/`에 있다. `npm install` 후 실행한다. Chrome 설치가 필요하다.

```bash
node 검증도구/validate.mjs 9반_P286_김진수_전진만할게요-API.yml 9반_P286_김진수_전진만할게요-DB.dbml
../../web/backend/.venv/bin/python 검증도구/contract_dump.py ../../web/backend /tmp/calls.json
node 검증도구/contract_check.mjs 9반_P286_김진수_전진만할게요-API.yml /tmp/calls.json
node 검증도구/pdf.mjs "$PWD/개요PDF/slides.html" "$PWD/9반_P286_김진수_전진만할게요-개요.pdf" /tmp/prev
```

- `contract_dump.py` — 임시 DB로 실제 FastAPI 앱을 띄워 27개 동작 전부와 주요 오류(400·401·403·404·409)를 호출해 응답을 저장한다. AI 키는 비워 대체 경로로 동작한다.
- `contract_check.mjs` — 저장한 응답을 명세 스키마로 검증한다. **선언하지 않은 필드도 실패로 처리**한다. 일부러 필드를 추가·타입을 바꾼 응답으로 실패하는 것을 확인했다(역검증).
- `capture.mjs` / `recapture.mjs` — 실행 중인 웹(5180)을 조작해 화면을 캡처한다. 실제 AI 호출이 일어난다.
- `render_tools.mjs` — DBML → ERD 이미지(mermaid, 로컬), 로컬 Swagger UI 캡처.

## 4. 2026-09-16 수정 기록

- **개요 PDF 조판** — `slides.html` body 에 `word-break: keep-all; overflow-wrap: anywhere;` 추가.
  7쪽 액터 표에서 "사용자(비로그인)"이 "비로그/인)"으로 단어 중간에서 잘리던 문제를 고쳤다. 한국어는 어절 단위로 줄바꿈하는 게 맞다.
  부수 효과로 12쪽에 있던 가로 넘침 1건(`DIV.ui`, 52px)도 사라졌다. 전 슬라이드 가로 넘침 0 확인 후 PDF 재생성(25쪽 유지, 이미지 누락 0).
- **재검증** — `validate.mjs` 통과(24경로·27동작·스키마 54 / 20테이블·27관계·15Enum·그룹5), 계약 검증 53건 27/27 동작 불일치 0, pytest 14 · vitest 20 · tsc 오류 0 · vite build 성공.
- **DB.dbml 은 변경 없음** (위 경고 항목 참고).

## 5. 이전 초안

`기획/이전기록/제출초안/`의 `design.yml`·`design.json`·`design.dbml`은 구현 전 초안(서비스명 “면허핏”, 경로 불일치)이다. **제출하지 않는다.** 이력 보존을 위해 지우지 않았다.
