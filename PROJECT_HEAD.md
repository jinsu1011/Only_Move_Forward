# 전진만할게요 — 프로젝트 헤드 문서

> 이 파일이 프로젝트의 기준점이다. 이어받기는 `이어서작업.md`, 진행 상황은 `기획/PROGRESS.md`.
> 최종 수정: 2026-09-16

## 0. 지금 상태 요약

- 웹: **동작.** 필기 연습·오답 해설, 센서/키보드 주행(기본조작·도로 A코스), 결과·AI 리포트. FastAPI + SQLite, React + Three.js
- 센서: Arduino UNO + MPU-6050 → Web Serial 직통. roll 로 좌/중앙/우 조향, 평활·튐 제거·유지 시간 보정(둔감/보통/민감)
- 주행 판정: **연습용.** 사건은 누적 기록하고 완주까지 계속 주행. 공식 채점(A) 활성 항목 0개
- AI: 회사 제공 GPT(OpenAI 호환). 실제 해설·리포트 생성 확인. 키가 없으면 규칙 기반 요약
- 제출물: `제출/최종본/` 에 API.yml·DBML(실제 코드 기준, 계약 검증 불일치 0)과 개요 PDF 25쪽. **dbdiagram.io·editor.swagger.io 실제 사이트 확인 완료(오류 0)**, 발표 대본 작성 완료
- **사용자 확인 필요**: 센서 좌우·민감도 실측 · 고유번호 P286 · 발표 리허설 · 업로드

## 1. 제품 정의

1·2종 보통 운전면허 준비자가 필기 오답과 주행 연습 기록을 함께 돌아보고, 근거 있는 AI 설명으로 다음 연습을 정하는 **학습 보조 서비스**. 공식 시험·실차교육을 대체하지 않는다.

## 2. 확정한 결정

| 항목 | 결정 |
|---|---|
| 서비스명 | **전진만할게요** (2026-09-15 사용자 결정) |
| 조작 — 센서 모드 | MPU-6050 roll 좌우 기울기 = 조향, 방향키 ↑ 전진 · ↓ 후진 |
| 조작 — 키보드 모드 | W 전진 · S 후진 · A/D 조향. 동시 입력은 정지/중앙 |
| 공통 키 | R 즉시 정지·다시 정렬, Esc 일시정지 → 한 번 더 누르면 기록 저장 후 나가기 |
| 센서 보정 | 원시 roll → 한 샘플 튐 제거 → 지수 평활 → 히스테리시스(진입/복귀) → 방향 유지 시간. 기본 **보통 15°/8°·150ms·120ms** (실측 후 확정) |
| 정렬 반복 줄이기 | 같은 탭에서 30분 안의 정렬값은 다시 쓰고 중앙 복귀(0.4초)만 확인. 키보드 확인도 탭 단위로 기억. R 은 처음부터 |
| 주행 사건 | 실격 해당 사건도 **즉시 종료하지 않고 누적**. 도로 이탈은 가까운 차로로 복귀. 끝나는 경우는 완주·제한 시간·Esc 나가기 |
| 결과 상태 | 완주 + 실격 해당 사건 0 → 완주 / 1건 이상 → 완주·실격 사유 포함 / 그 외 → 미완료 |
| 판정 검증 | 서버가 입력 기록을 같은 엔진(sim-1.0.0)으로 재생해 사건을 확정. 브라우저·서버 엔진은 골든 기록으로 교차 검증 |
| 공식 채점 범위 | 별표26 57항목 조사. A(자동 채점) 0 · B(연습 표시) · C(안내만). 검증 전 A 활성화 금지 |
| 필기 문항 | 자체 제작 연습 문항(PRACTICE). 공식 문제은행은 이용 조건·정답 검수 전 사용하지 않음 |
| AI | 정답·규칙은 데이터가 결정, AI 는 설명만. 인용 id·수치를 서버가 검증 |
| 제출 명세 | **실제 코드가 원본.** API.yml·DBML 은 코드에 맞춰 손으로 관리하고, `검증도구/` 계약 검증으로 불일치를 확인한다 |
| 제출 파일명 | `9반_P286_김진수_전진만할게요-{개요.pdf, API.yml, DB.dbml}` (반·번호·이름은 첫 예시 값, 사용자 확인) |

## 3. 폴더 구조

```
미니프로젝트1_ver(2)/
├── README.md              # 저장소 소개
├── PROJECT_HEAD.md        # 이 파일
├── 이어서작업.md           # 다른 노트북·새 대화에서 이어받기
├── 시작.sh / 정리.sh       # 설치물 점검·재설치 / 서버 종료
├── 제출/최종본/            # 제출 3종 · 체크리스트 · 발표대본 · 개요PDF 원본 · 자료(캡처·ERD·Swagger) · 검증도구
├── web/
│   ├── backend/           # FastAPI · SQLite · 주행 재생 검증 · LLM
│   └── frontend/          # React · Three.js · Web Serial
├── hardware/firmware/     # Arduino UNO + MPU-6050 펌웨어
├── data/
│   ├── road-rule-research.json  # 별표26 57항목 조사 데이터(서버 시드)
│   └── local/             # SQLite DB (깃 제외)
├── 기획/
│   ├── PROGRESS.md        # 완료·진행 중·다음 작업
│   ├── 조사/              # 공식 자료·참고자료 검토·프로토타입 분석
│   ├── 설계/              # 도로주행 규칙 지원 범위(A/B/C)
│   ├── 평가/              # 발표 계획
│   ├── 근거/              # 별표26 원문 추출·참고자료 해시·프로토타입 기준선 (수업 자료 추출본은 깃 제외)
│   └── 이전기록/           # 구현 전 설계 초안·평가 감사·이어가기 프롬프트 (현재 기준 아님)
├── driving_game/          # 기존 파이썬 프로토타입 원본(수정하지 않음)
└── 참고자료/               # 수업 배포 자료·법령 원문(깃 제외)
```

## 4. 지키는 원칙

1. 연습 판정을 공식 채점이라고 말하지 않는다. 측정하지 않은 행동(거울·방향지시등·반응시간)을 평가하지 않는다.
2. 과거 기록을 덮어쓰지 않는다. 재시도는 새 기록.
3. 비밀키는 `web/backend/.env` 에만. 깃·화면·캡처에 넣지 않는다.
4. 구현한 것과 설계만 한 것을 문서·발표에서 구분한다. 실측하지 않은 센서 동작을 완료로 쓰지 않는다.
5. 제출물 업로드는 사용자가 한다. 외부 사이트(dbdiagram·Swagger Editor) 확인은 2026-09-16 사용자 지시로 대신 수행했고, 결과를 `제출/최종본/체크리스트.md` 에 남겼다.
6. API·DB 를 바꾸면 `제출/최종본/` 명세도 함께 고치고 계약 검증을 다시 돌린다.
